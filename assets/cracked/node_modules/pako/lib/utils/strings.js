var u = require("./common");
var h = true;
var a = true;
try {
  String.fromCharCode.apply(null, [0]);
} catch {
  h = false;
}
try {
  String.fromCharCode.apply(null, new Uint8Array(1));
} catch {
  a = false;
}
var c = new u.Buf8(256);
for (var d = 0; d < 256; d++) {
  c[d] = d >= 252 ? 6 : d >= 248 ? 5 : d >= 240 ? 4 : d >= 224 ? 3 : d >= 192 ? 2 : 1;
}
function p(f, S) {
  if (S < 65537 && (f.subarray && a || !f.subarray && h)) {
    return String.fromCharCode.apply(null, u.shrinkBuf(f, S));
  }
  var _ = "";
  for (var x = 0; x < S; x++) {
    _ += String.fromCharCode(f[x]);
  }
  return _;
}
c[254] = c[254] = 1;
exports.string2buf = function (f) {
  var S;
  var _;
  var x;
  var w;
  var g;
  var b = f.length;
  var m = 0;
  for (w = 0; w < b; w++) {
    if (((_ = f.charCodeAt(w)) & 64512) == 55296 && w + 1 < b && ((x = f.charCodeAt(w + 1)) & 64512) == 56320) {
      _ = 65536 + (_ - 55296 << 10) + (x - 56320);
      w++;
    }
    m += _ < 128 ? 1 : _ < 2048 ? 2 : _ < 65536 ? 3 : 4;
  }
  S = new u.Buf8(m);
  w = g = 0;
  for (; g < m; w++) {
    if (((_ = f.charCodeAt(w)) & 64512) == 55296 && w + 1 < b && ((x = f.charCodeAt(w + 1)) & 64512) == 56320) {
      _ = 65536 + (_ - 55296 << 10) + (x - 56320);
      w++;
    }
    if (_ < 128) {
      S[g++] = _;
    } else {
      if (_ < 2048) {
        S[g++] = _ >>> 6 | 192;
      } else {
        if (_ < 65536) {
          S[g++] = _ >>> 12 | 224;
        } else {
          S[g++] = _ >>> 18 | 240;
          S[g++] = _ >>> 12 & 63 | 128;
        }
        S[g++] = _ >>> 6 & 63 | 128;
      }
      S[g++] = _ & 63 | 128;
    }
  }
  return S;
};
exports.buf2binstring = function (f) {
  return p(f, f.length);
};
exports.binstring2buf = function (f) {
  var S = new u.Buf8(f.length);
  for (var _ = 0, x = S.length; _ < x; _++) {
    S[_] = f.charCodeAt(_);
  }
  return S;
};
exports.buf2string = function (f, S) {
  var _;
  var x;
  var w;
  var g;
  var b = S || f.length;
  var m = new Array(b * 2);
  for (_ = x = 0; _ < b;) {
    if ((w = f[_++]) < 128) {
      m[x++] = w;
    } else if ((g = c[w]) > 4) {
      m[x++] = 65533;
      _ += g - 1;
    } else {
      for (w &= g === 2 ? 31 : g === 3 ? 15 : 7; g > 1 && _ < b;) {
        w = w << 6 | f[_++] & 63;
        g--;
      }
      if (g > 1) {
        m[x++] = 65533;
      } else if (w < 65536) {
        m[x++] = w;
      } else {
        w -= 65536;
        m[x++] = w >> 10 & 1023 | 55296;
        m[x++] = w & 1023 | 56320;
      }
    }
  }
  return p(m, x);
};
exports.utf8border = function (f, S) {
  var _;
  if ((S = S || f.length) > f.length) {
    S = f.length;
  }
  _ = S - 1;
  while (_ >= 0 && (f[_] & 192) == 128) {
    _--;
  }
  if (_ < 0 || _ === 0) {
    return S;
  } else if (_ + c[f[_]] > S) {
    return _;
  } else {
    return S;
  }
};